// Package duration is part of the HILIOS Operator. See docs/architecture.md for
// the package's place in the reconcile loop. The package is intentionally
// small and free of cluster API access except where explicitly noted in the
// individual file headers.
package duration
