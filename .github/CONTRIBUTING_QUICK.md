# Contributing in 60 seconds

1. Fork or branch off `main`.
2. `make test lint helm-lint` before pushing.
3. Run `git commit -s` so the DCO line is included.
4. Push and open a pull request from your branch into `main`.
5. Wait for CI checks; the labeler workflow will tag the PR automatically.
