apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: infra-rebalance
  namespace: infra
spec:
  targetSelector:
    matchLabels:
      env: infra
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
