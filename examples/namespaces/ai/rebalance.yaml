apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: ai-rebalance
  namespace: ai
spec:
  targetSelector:
    matchLabels:
      env: ai
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
