apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: ml-rebalance
  namespace: ml
spec:
  targetSelector:
    matchLabels:
      env: ml
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
