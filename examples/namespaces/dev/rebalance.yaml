apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: dev-rebalance
  namespace: dev
spec:
  targetSelector:
    matchLabels:
      env: dev
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
