apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: low-shipping-rebalance
  namespace: default
spec:
  targetSelector:
    matchLabels:
      app: shipping
      tier: low
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
