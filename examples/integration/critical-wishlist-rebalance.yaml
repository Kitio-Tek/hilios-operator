apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: critical-wishlist-rebalance
  namespace: default
spec:
  targetSelector:
    matchLabels:
      app: wishlist
      tier: critical
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
