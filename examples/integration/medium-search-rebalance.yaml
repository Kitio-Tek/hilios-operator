apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: medium-search-rebalance
  namespace: default
spec:
  targetSelector:
    matchLabels:
      app: search
      tier: medium
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
