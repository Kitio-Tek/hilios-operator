apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: medium-inventory-rebalance
  namespace: default
spec:
  targetSelector:
    matchLabels:
      app: inventory
      tier: medium
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
