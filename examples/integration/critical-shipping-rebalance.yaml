apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: critical-shipping-rebalance
  namespace: default
spec:
  targetSelector:
    matchLabels:
      app: shipping
      tier: critical
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
