apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: linode-zone-spread
  namespace: default
  annotations:
    hilios.io/cloud: linode
spec:
  targetSelector:
    matchLabels:
      cloud: linode
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
