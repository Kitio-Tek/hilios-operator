apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: suse-zone-spread
  namespace: default
  annotations:
    hilios.io/cloud: suse
spec:
  targetSelector:
    matchLabels:
      cloud: suse
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
