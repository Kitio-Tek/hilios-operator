apiVersion: resilience.hilios.io/v1alpha1
kind: ResiliencePolicy
metadata:
  name: oracle-managed
  namespace: default
  annotations:
    hilios.io/cloud: oracle
spec:
  targetSelector:
    matchLabels:
      cloud: oracle
      hilios.io/enabled: "true"
  verifications:
    - kind: RestoreVerification
      intervalSeconds: 21600
      freshnessSeconds: 86400
  slo:
    recoveryTimeSeconds: 600
    maxReplicaSkew: 1
