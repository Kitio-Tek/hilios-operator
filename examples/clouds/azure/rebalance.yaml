apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: azure-zone-spread
  namespace: default
  annotations:
    hilios.io/cloud: azure
spec:
  targetSelector:
    matchLabels:
      cloud: azure
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
