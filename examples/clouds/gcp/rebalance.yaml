apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: gcp-zone-spread
  namespace: default
  annotations:
    hilios.io/cloud: gcp
spec:
  targetSelector:
    matchLabels:
      cloud: gcp
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
