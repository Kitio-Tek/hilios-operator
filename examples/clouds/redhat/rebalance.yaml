apiVersion: resilience.hilios.io/v1alpha1
kind: RebalanceCheck
metadata:
  name: redhat-zone-spread
  namespace: default
  annotations:
    hilios.io/cloud: redhat
spec:
  targetSelector:
    matchLabels:
      cloud: redhat
  topologyKey: topology.kubernetes.io/zone
  maxSkew: 1
  dryRun: true
