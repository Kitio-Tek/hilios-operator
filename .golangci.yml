version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - errcheck
    - goconst
    - gocyclo
    - godot
    - govet
    - ineffassign
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - whitespace
  settings:
    gocyclo:
      min-complexity: 25
    goconst:
      # Only flag repeated literals that appear five or more times;
      # the default of three churns on common short strings like "name".
      min-occurrences: 5
    godot:
      scope: declarations
      period: true
    revive:
      rules:
        - name: comment-spacings
    staticcheck:
      # SA1019 surfaces deprecation notices for upstream APIs. We track
      # those manually in the roadmap; the suppression keeps our lint
      # signal focused on actionable findings.
      checks:
        - all
        - -SA1019
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: api/.*
      - linters:
          - dupl
          - lll
        path: internal/.*
      - linters:
          - revive
        source: ^//\+
      - linters:
          - dupl
          - goconst
          - lll
        path: _test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-same-issues: 0
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
